# ThoughtDAG 0.4.14-rc2.28

运行环境：DSH 0.1.5-rc.2 / web；Core 0.3.12-rc2.29。无需 Maintenance、Bridge、Launcher 或普通贴纸。

工作区等待有超时、取消和 fiber 清理；卸载后不处理晚到网络结果。Core 依赖切换会拒绝旧操作；图端口缓存随 owner 清理。可选 sessionWriteAccess 曾出现后，断开时保留读取、暂停写入，恢复后重新核验。

35 项 DSH Node 回归通过，构建包含类型检查；真实运行期动态重载和 UI 未验收。正常停机、备份后升级；既有图数据仍走 Core 公共端口。
