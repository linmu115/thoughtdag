# 本轮开发历程草稿（未索引）

来源：当前用户公开任务「按文档对各个插件进行架构升级」及本轮工具验证。未绑定可展开的宿主事件索引，不伪造 history-event 回执。

按九份已确认文档实施，保留用户要求暂缓的 DAG adapter、GPT Compat 和多实例绑定范围。先建立本地能力和通用接入口，再完成受管门禁、安装检查、候选打包及说明。

回归中识别了旧测试的镜像自动开启/Engine 自动启动假设、版本清单断言和打包缺少共享文件问题。修订生产默认行为后，相应旧测试按新的公开合同复验，历史失败保留在交付报告。恢复测试还发现旧运行读取后来更新的共享目录导致条目丢失，改为保留该运行自己的固定目录。

ThoughtDAG 的 pnpm 自动依赖检查曾触发异种包管理器安装；随后停止该流程，用原 npm lock 和 legacy peer 模式恢复依赖，直接调用构建脚本，未引入新的依赖版本清单。

结果与具体测试数字以交付报告为准；没有安装真实实例或更改实际会话/Vault。原有未提交改动保持，不将它们当作本轮首次产出。


## DAG 混合环境阻断与用户纠正

来源：当前用户注释确认 DAG 仅依赖 Core，并指出已同步图应从 Maintenance 真源恢复。旧路径因 Maintenance 服务存在而抢占本地接口；已补充公共端口与 adapter 恢复测试。本轮纠正：DAG 无条件使用 Core 公共会话数据/引用端口，删除旧 Maintenance 优先选择。主干能力必须通过实际读取检查；图合法性和增删仍由 DAG 负责。10 项针对性测试通过，实际 WebUI 待修复 Maintenance .35 注册识别后验证。 当前来源尚未单独绑定事件索引，本段是过程草稿。
