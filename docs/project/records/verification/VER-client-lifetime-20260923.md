---
id: VER-client-lifetime-20260923
kind: verification
title: 客户端生命周期合成验证
status: current
summary: 35 项 DSH Node 测试通过，包含等待取消、提供者切换、写入许可断开与端口缓存重载；实机及 UI 未验收。
---

2026-09-23，在 ThoughtDAG 仓库执行 `node --test dsh/tests/*.test.mjs`，35/35 通过。客户端 10 项测试覆盖现有引用及打开行为，以及服务已就绪、延迟就绪、同步回调、等待超时、卸载中止、传输忽略取消后迟到、引用准备中卸载、会话刷新中卸载、Core 切换期间的 staging 和删除防护。

宿主新增测试验证：没有发生写请求时仍观察到许可提供者的出现与移除；断开后拒绝新建但允许读取；重注册允许写入；旧许可校验迟到不授权新建；同一 ctx 卸载重挂后实际调用新图数据/来源提供者。

夹具模拟浏览器消息和服务，验证取消信号、后续服务调用计数、无卸载后回传和等待 fiber 释放；不等同于真实浏览器或实际 Cordis loader 全生命周期验收。宿主路由测试仍使用已有模拟服务，未据此确认官方注册返回值合同。未修改或读取用户会话正文；未安装新版，UI 未验收。
