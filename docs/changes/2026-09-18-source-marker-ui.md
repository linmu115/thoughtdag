# 会话引用尾标样式（2026-09-18）

按确认方案将会话引用尾标改为细线引号、22px 透明点击区和浅底 hover；主动跳转采用 arrow-up-left，解除关系采用 unlink。多行尾标的纵坐标改为末行。清除宿主样式字符串中复制遗留的普通贴纸/知识面板样式，避免覆盖其他插件的单层编辑界面。

最终 DSH 构建通过，ownership 3 文件/30 项测试通过；本轮 TypeScript 检查通过。普通贴纸/Core 的实际组件浏览器检查不能替代本插件宿主尾标的真实 UI 验收。ThoughtDAG 尾标真实实例 UI 未验收；源码未安装、未发布、未改变运行实例。
