# 全局网络与固定材料预览修复

本次处理系统审计 F02、F10 前端部分、F11 前端部分。版本号暂时保持不变，由组合部署统一调整。

- 全局网络打开会话时先解析逻辑身份，再向宿主提交原生会话身份。`parentRequest` 按操作约束参数，宿主在请求前拒绝缺失或混用身份，避免把 `undefined` 发送给会话解析接口。
- 保存的材料明确显示正在读取固定来源版本。首次展开继续传保存的版本和回复锚点，后续只传固定版本游标，不重新选择来源。Maintenance 负责读取仍保留的旧版本；版本不可用时显示错误，不静默换成最新回复。
- 网络增加独立“上下文引用”类型，可按引用、选文及会话标题搜索；引用项显示来源、目标和已发送/已解除状态。支持按会话查询入向和出向引用，来源与目标都有完整会话跳转入口。默认图直接显示返回的权威引用边，不依赖先查询影响。
- 翻页保持上一查询的筛选快照，编辑尚未提交的新查询不会把旧游标用于新条件。图只呈现前 100 个结果及相关端点，列表继续分页。

沿用现有 DSH 界面风格与固定切换按钮位置。影响处理仍只准备用户选中的引用和重新讨论草稿，不自动发送或调用模型。

## 验证

- 宿主、客户端与图模型：36 个测试通过。
- 插件构建通过，包含完整 TypeScript 编译门禁。
- 合成浏览器：10 项流程通过、页面错误为 0。使用实际构建 SPA、真实 iframe 消息、实际宿主 shim 和 HTTP 路由；仅 Maintenance/DSH 服务与数据为合成 fixture。
- 新浏览器场景包括：网络会话点击传原生 ID；错误字段在查询前被拒绝；来源版本从 1 追加到 2 后材料仍请求版本 1 并沿其游标翻页；独立引用搜索、未提交搜索不影响分页、入向/出向筛选、目标跳转和解除状态。
- 既有切换按钮在 1440、900、390 宽度保持位置，画布状态、CAS 冲突、删除恢复、原草稿与人工准备流程仍通过。

本次测试 stdout、结果及截图位于 `D:/AI/DeepSeekHarness-Plugin/artifacts/system-fixes-20260914/thoughtdag/`：`unit.log`、`build.log`、`browser.log`、`browser/result.json`、`browser/knowledge-network.png`。初次类型检查暴露的可选字段窄化问题已修复，后续完整构建通过。

没有操作真实副本、真实会话或 Vault，也没有运行真实模型。后端旧版本读取与引用查询由 Maintenance 的配套改动和测试验证。
