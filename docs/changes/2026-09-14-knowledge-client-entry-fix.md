# 副本浏览器入口修复

DSH 插件 0.4.14-rc2.3：画布顶部的单个“对话”返回按钮改为与会话页一致的“对话｜思维图”切换开关。进入画布时高亮“思维图”，返回时高亮“对话”；两个入口共享状态和样式。已选视图重复点击不重置画布，来回切换复用现有 iframe。被覆盖的会话顶栏开关从辅助技术树中隐藏。生产构建、20 项宿主测试和 7 项浏览器场景通过，其中新增两端开关及返回后保留画布验证。

## 安装验证

先提交本地源码，再安装到 0.1.5-rc.2 副本。实际宿主与浏览器验证回执位于 D:/AI/DeepSeekHarness-Plugin/artifacts/knowledge-network-rc2-20260914/client-fix，安装结果另行记录。旧数据迁移由用户选择会话后操作；影响传播只准备用户选中的重答，不自动发送或运行模型。
