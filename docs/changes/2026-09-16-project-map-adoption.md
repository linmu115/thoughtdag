# 项目地图接入记录

日期：2026-09-16。

## 改动

建立本项目长期地图，复用当前 RC2 需求、使用文档与模块说明的精确标题。补充面向开发者的对象、协作接口、实际能力、资料选择理由与薄跨项目 ID 关联。实现与验证分开；已删除功能、未确认建议和历史验收不混入当前交付承诺。

每图包含原生 Archify 架构和流程图，使用已安装 maintain-project-map Skill 导出 A/B/C 阅读页面。本地 views 由 .gitignore 排除，地图及图源继续由 Git 管理。README 增加可发现的入口。

## 本次验证

- 所有登记 Markdown 标题可唯一绑定；记录身份和本地关系检查通过，无警告。
- 跨地图 project_id / record_id 均在本机登记解析并核对存在。
- 架构和流程分别通过 Archify 9/9 检查，0 错误、0 警告；保留原版、嵌入版和交付回执。
- 关键词检索及有界读取返回当前条目；入口、文档数据、架构和流程 HTTP 请求均为 200。
- 地图 Skill 的独立发布副本通过 74 项测试及官方 quick_validate；这不是本项目产品回归。
- 浏览器控制接口在枚举阶段报 nodeRepl.fetch request failed，无法完成本次真实浏览器视觉检查。HTTP/原生交付通过不替代该检查。

## 边界

本次没有部署插件、运行真实模型请求或改动真实会话与 Vault。历史测试仍按原报告版本理解。源码基线记录于 docs/project/source-baseline.json；修改 README 仅增加地图入口，未改变原有绑定章节。地图完成后仅在本地提交，本次不推送插件仓库。
