# 独立构建与 RC2 环境说明

用户确认（2026-09-22）：补齐 CLI 首次启用和只读验证教程，消除源码构建对作者工作树的要求，DAG/Bridge 的 Core 依赖对齐 0.3.12-rc2.28；五个插件统一标注 DSH 0.1.5-rc.2 实例环境。Sidechat 仍不在本次修改范围。

使用锁定工具链与仓库内 SDK 快照；真实实例不升级，不改变 Vault 绑定或同步范围。验证在独立干净目录进行；使用 Node.js 24.7.0 和锁定包管理器安装、构建及打包。

## 实现与验证

Node 测试 69 项、Vitest 测试 27 项通过；DSH 构建和打包通过。移除未使用的 Maintenance 开发依赖，显式声明 esbuild，使用 npm 11.5.1 和锁文件。Core required peer 对齐 0.3.12-rc2.28。DSH 插件版本 0.4.14-rc2.27。

运行包入口检查通过；运行清单不包含作者工作树路径或开发依赖。全部源码、README 与安装教程统一声明 DSH 0.1.5-rc.2 / web profile。

## 过程与边界

早期干净安装暴露了遗留 sibling/link 依赖、移动后的锁文件条目和旧版本断言；已以仓库内 SDK、重新锁定依赖和同步合同断言解决。Core 原生宿主集成与 Bridge 跨仓库 HTTP 联测改为显式环境变量入口，独立测试不再隐含外部源码目录；两类可选集成测试也实际通过。

这是本次用户要求及命令结果的过程草稿，尚未绑定可展开的历史事件索引。未部署到当前运行实例；未改动 Vault 绑定、笔记或同步名单；真实 UI 和本轮全部业务场景未验收。
