# 思维图显示当前会话名称

0.4.14-rc2.13 修复旧主干和会话卡片继续显示创建时 ID 的问题，配套 Maintenance 插件 0.2.26-rc2.22 / Engine 0.1.33-rc2.26。

已绑定主干的名称和会话卡片跟随当前会话元数据，已有图无需删除重建。左侧图列表也使用相同规则。纯机器标识不再用作缺失名称的兜底，确实没有可读名称时显示“未命名会话”。

会话名称请在会话页修改；图中不再维护另一份会话卡片名称。未绑定草稿、占位卡片和材料卡仍可自行命名。后台刷新时会话名更新，同时保留未保存的位置、缩放、材料名称和待确认连线。

名称在读取时投影，每张图最多一次批量名称查询，不读取对话正文，不扫描全部原生会话，不增加图修订或复制备份。固定来源版本、读取截止与关系身份保持不变。

验证涵盖已保存 ID、会话重命名、图列表、缺失名称、材料名称保留、未保存布局合并及现有图操作。实际副本安装另以部署回执确认。

## 副本交付

2026-09-15 已安装到 0.1.5-rc.2 副本并由 Launcher 重启。实际 Engine 接口核对 5 张主干图和 7 张会话卡片名称通过；更新前只读诊断确认其中 2 个图标题、5 个卡片 label 使用旧名称。在线前端文件与本批安装包一致。

18 项浏览器检查和 18 项插件/布局同步测试通过，Maintenance 名称及接口相关 14 项测试通过。合成浏览器检查不等于操控真实用户画布；实际数据验证使用只读接口。会话历史、版本、原有引用数据和主实例配置保持不变。此前的连续滑动按钮一并安装。
