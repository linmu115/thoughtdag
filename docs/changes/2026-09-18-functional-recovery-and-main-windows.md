# 中间主区思维图与维护恢复 · 2026-09-18

用户确认：左侧会话导航作用于活动主窗，思维图只占中间主区，左右外栏可用；移除新建未绑定草稿/草稿列表入口但保留存量数据。功能任务同时负责host顶栏结构及紧凑视觉。

host overlay按原生会话中心区域测量；只对被覆盖的会话区域设inert，不再禁用整个body。每个独立客户端的模式开关在顶栏正中。图侧栏/草稿入口移除，已有草稿API、数据、冲突恢复保存均保留。图顶栏采用左右标题/紧凑图标，黑底白色保存图标，显式round角型，修订在title中。

维护服务缺失或离线时mainGraph不再报告假就绪；恢复后重新检查并打开当前主干。会话贴纸请求不回退到本地第二存储，网络失败明确未确认保存，非JSON响应不会被视作成功；不自动重放写操作。

验证：TypeScript和DSH生产构建通过；managed-host 11项（含无SM/离线/恢复），client9项，graph-ui5项浏览器测试，以及dual-main-windows1项真实浏览器合成宿主测试。双窗用相邻BetterSidebar源码构建，验证独立草稿/模式、会话路由、左右栏、收起保留及无pageerror。原任务亦独立浏览器复核合成预览并给出顶栏反馈，已实施。未部署，真实DSH编辑器/历史引用/跨端写入仍未验收。

开发历程：旧浏览器测试依赖已移除的草稿入口，已改为会话选择→ensure主干，继续验证拖拽、保存、窄屏菜单和权限分离。合成预览不会接入真实业务数据。
